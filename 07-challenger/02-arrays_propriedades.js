// PROPRIEDADES DE ARRAYS
let fruits = ["Banana", "Orange", "Apple", "Mango"];

// PROPRIEDADES LENGTH
console.log(fruits.length);

// TESTES
let qtd = fruits.length;

while (qtd > 0) {
  console.log(fruits[--qtd]);
}
